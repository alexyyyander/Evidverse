apiVersion: apps/v1
kind: Deployment
metadata:
  name: yivid-backend
  namespace: yivid
spec:
  replicas: 2
  selector:
    matchLabels:
      app: yivid-backend
  template:
    metadata:
      labels:
        app: yivid-backend
    spec:
      containers:
        - name: backend
          image: ghcr.io/your-org/yivid-backend:cloud_version
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: yivid-config
            - secretRef:
                name: yivid-secret
          readinessProbe:
            httpGet:
              path: /api/v1/health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /api/v1/health
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 20

