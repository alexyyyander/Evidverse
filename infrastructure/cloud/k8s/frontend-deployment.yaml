apiVersion: apps/v1
kind: Deployment
metadata:
  name: yivid-frontend
  namespace: yivid
spec:
  replicas: 2
  selector:
    matchLabels:
      app: yivid-frontend
  template:
    metadata:
      labels:
        app: yivid-frontend
    spec:
      containers:
        - name: frontend
          image: ghcr.io/your-org/yivid-frontend:cloud_version
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
          env:
            - name: NEXT_PUBLIC_APP_MODE
              value: "cloud"
            - name: NEXT_PUBLIC_API_ORIGIN
              value: "https://api.yivid.example"
