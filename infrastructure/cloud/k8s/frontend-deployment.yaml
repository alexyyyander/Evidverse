apiVersion: apps/v1
kind: Deployment
metadata:
  name: evidverse-frontend
  namespace: evidverse
spec:
  replicas: 2
  selector:
    matchLabels:
      app: evidverse-frontend
  template:
    metadata:
      labels:
        app: evidverse-frontend
    spec:
      containers:
        - name: frontend
          image: ghcr.io/your-org/evidverse-frontend:cloud_version
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
          env:
            - name: NEXT_PUBLIC_APP_MODE
              value: "cloud"
            - name: NEXT_PUBLIC_API_ORIGIN
              value: "https://api.evidverse.example"
