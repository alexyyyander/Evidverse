apiVersion: v1
kind: Service
metadata:
  name: evidverse-backend
  namespace: evidverse
spec:
  selector:
    app: evidverse-backend
  ports:
    - name: http
      port: 8000
      targetPort: 8000

