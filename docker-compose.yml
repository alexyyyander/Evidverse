services:
  # --- Infrastructure Services ---
  
  db:
    image: postgres:15-alpine
    container_name: evidverse-db
    restart: always
    environment:
      - POSTGRES_USER=evidverse
      - POSTGRES_PASSWORD=evidverse_password
      - POSTGRES_DB=evidverse_db
    ports:
      - "5432:5432"
    volumes:
      - evidverse_postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: evidverse-redis
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - evidverse_redis_data:/data

  rabbitmq:
    image: rabbitmq:3-management-alpine
    container_name: evidverse-queue
    restart: always
    environment:
      - RABBITMQ_DEFAULT_USER=guest
      - RABBITMQ_DEFAULT_PASS=guest
    ports:
      - "5672:5672"   # AMQP
      - "15672:15672" # Management UI

  minio:
    image: minio/minio
    container_name: evidverse-minio
    restart: always
    command: server --console-address ":9001" /data
    environment:
      - MINIO_ROOT_USER=minioadmin
      - MINIO_ROOT_PASSWORD=minioadmin
    ports:
      - "9000:9000" # API
      - "9001:9001" # Console
    volumes:
      - evidverse_minio_data:/data

  createbuckets:
    image: minio/mc
    depends_on:
      - minio
    entrypoint: >
      /bin/sh -c "
      /usr/bin/mc config host add myminio http://minio:9000 minioadmin minioadmin;
      /usr/bin/mc mb myminio/evidverse-bucket;
      /usr/bin/mc anonymous set public myminio/evidverse-bucket;
      exit 0;
      "

  # --- Application Services (Optional for Dev) ---
  # Uncomment these to run the full stack in Docker
  
  # backend:
  #   build: ./backend
  #   container_name: evidverse-backend
  #   command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
  #   volumes:
  #     - ./backend:/app
  #   ports:
  #     - "8000:8000"
  #   environment:
  #     - DATABASE_URL=postgresql://evidverse:evidverse_password@db/evidverse_db
  #     - REDIS_URL=redis://redis:6379/0
  #     - CELERY_BROKER_URL=amqp://guest:guest@rabbitmq:5672//
  #     - CELERY_RESULT_BACKEND=redis://redis:6379/0
  #     - S3_ENDPOINT_URL=http://minio:9000
  #     - S3_ACCESS_KEY=minioadmin
  #     - S3_SECRET_KEY=minioadmin
  #     - S3_BUCKET_NAME=evidverse-bucket
  #   depends_on:
  #     - db
  #     - redis
  #     - rabbitmq
  #     - minio

  # frontend:
  #   build: ./frontend
  #   container_name: evidverse-frontend
  #   volumes:
  #     - ./frontend:/app
  #     - /app/node_modules
  #   ports:
  #     - "3000:3000"
  #   environment:
  #     - NEXT_PUBLIC_API_URL=http://localhost:8000
  #   depends_on:
  #     - backend

volumes:
  evidverse_postgres_data:
  evidverse_redis_data:
  evidverse_minio_data:
